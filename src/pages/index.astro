---
---

<script is:inline>
  const userLang = navigator.language || navigator.userLanguage;
  const savedLang = localStorage.getItem('lang');
  if (userLang.startsWith('es') && savedLang !== 'en') {
    window.location.href = '/es';
  } else {
	window.location.href = '/en';
  }

</script>